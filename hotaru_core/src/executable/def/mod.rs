//! Pre-registration route definitions (`AccessPointDef<P>`).
//!
//! The pre-registration type paired to the runtime post-registration
//! `AccessPoint<C, TS>` in `super::access`. The `Def` suffix
//! disambiguates at the type name, not just the module path.

// mod access_point;
// mod error;
// mod prepare;
//
// pub use access_point::{AccessPointDef, Endpoint, MiddlewareSlot, Outpoint, UrlMode};
// pub use error::BindError;
// pub(crate) use prepare::{PreparedAccessPoint, prepare_from_def};
//
// // Re-exported for convenience so downstream code can spell prepared
// // bindings through the def module in migration examples.
// pub use super::executable::ExecutableBinding;
