[package]
name = "h2per_hello"
version = "0.1.0"
edition = "2024"

[dependencies]
hotaru = { path = "../../hotaru" }
h2per = { path = "../../h2per" }
tokio = { version = "1", features = ["full"] }
once_cell = "1.17"
ctor = "0.4"
serde_json = "1.0"